query GetMoviesByGenre($genre: String, $minVoteAverage: Float) {
  movies(genre: $genre, minVoteAverage: $minVoteAverage, orderBy: "voteAverage", sort: DESC, limit: 20) {
    id
    title
    voteAverage
    genres
    posterPath
    overview
  }
}
