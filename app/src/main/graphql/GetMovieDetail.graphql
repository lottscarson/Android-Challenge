query GetMovieDetail($id: Int!) {
  movie(id: $id) {
    id
    title
    overview
    releaseDate
    voteAverage
    voteCount
    runtime
    budget
    genres
    posterPath
    director {
      id
      name
    }
    cast {
      name
      character
      order
      profilePath
    }
  }
}
